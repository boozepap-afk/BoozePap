/** @deprecated Import from email-transport. Kept for compatibility with any
 * server modules outside this checkout that still use the historical path. */
export { deliverEmail } from '@/lib/server/email-transport';
